{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "concurrently -k -n srv,dts -c blue,green \"bun --watch run src/index.ts\" \"tsc --watch --preserveWatchOutput\""
  },
  "dependencies": {
    "hono": "^4.10.8",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.1.2"
  }
}
